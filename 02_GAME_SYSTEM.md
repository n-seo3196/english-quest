# 02_GAME_SYSTEM - ゲームシステム仕様

## ゲームの基本構造

```
[スタート画面] → [ステージ1] → [ステージ2] → ... → [ステージ5] → [完了画面]
```

各ステージの中の流れ：

```
道を歩き始める
  ↓
ヒントアイテムを見つける（任意）
  ↓
踏むとヒントが1つ解放
  ↓
さらに進む
  ↓
3つの選択肢看板（ゲート）に到着
  ↓
正しい看板をくぐる → 正解 → 次のステージ
間違った看板をくぐる → 不正解 → 4タイル後退してやり直し
```

---

## 操作方法

### PC（キーボード）

| キー | 動作 |
|------|------|
| ↑ or W | 上に1タイル移動 |
| ↓ or S | 下に1タイル移動 |
| ← or A | 左に1タイル移動 |
| → or D | 右に1タイル移動 |

- キーを押し続けると連続移動（約100msごと）
- 草原（道の外）には移動できない

### スマホ（タッチD-pad）

- 画面左下に十字キーが表示される
- 各方向ボタンをタッチで移動
- 長押しで連続移動（約120msごと）
- `'ontouchstart' in window` でスマホを判定して自動表示

---

## マップシステム

### 無限スクロール方式

- マップに固定サイズはない
- プレイヤーの現在位置（worldY）を基準にタイルを動的に描画
- 画面に見えている範囲のタイルだけを毎フレーム計算して描画

### タイルの種類

| タイルID | 名前 | 見た目 | 歩行 |
|----------|------|--------|------|
| 0 | 草原 | 緑。草・花・木がランダム装飾 | 不可 |
| 1 | 道路 | 土色。小石の模様あり | 可能 |
| 2 | 縁石 | 灰茶色の石 | 可能 |

### タイル判定ロジック

```
col 0〜3   → 草原（歩行不可）
col 4      → 縁石（歩行可能）
col 5〜15  → 道路（歩行可能）
col 16     → 縁石（歩行可能）
col 17〜20 → 草原（歩行不可）
```

### 装飾の生成

`seededRand(col, row)` 関数で、同じ座標には毎回同じ装飾が出る（見た目の一貫性）：

- 確率 > 0.35 → 草のタフト（2本の細い線）
- 確率 > 0.78 → 花（赤 or 黄色の小さい丸）
- 確率 > 0.91 かつ端のほう → 木（幹 + 葉っぱ2段）

---

## オブジェクト配置

各ステージで以下のオブジェクトがプレイヤーの前方に配置される：

### ヒントアイテム

| 項目 | 値 |
|------|-----|
| 配置位置 | worldY = -8, -18, -28（プレイヤーの前方） |
| 横位置 | col 7〜13 のランダム |
| 最大数 | 3個（問題のヒント数と同じ） |
| 見た目 | 金色の星型 + 「?」マーク + 脈動するグロー |
| 取得方法 | プレイヤーが同じタイルを踏む |

### ゲート（選択肢看板）

| 項目 | 値 |
|------|-----|
| 配置位置 | worldY = -38（ヒントの先） |
| 3つのゾーン | col 5〜8, col 8〜12, col 12〜15 |
| 選択肢の並び | ランダムにシャッフル |
| 看板デザイン | 木の柱 + 看板ボード + A/B/Cバッジ |

---

## スコアシステム

### 得点計算

```
正解時のスコア = 基本点 + ヒントボーナス + コンボボーナス

基本点       = 100pt（固定）
ヒントボーナス = (3 - 使ったヒント数) × 50pt
                → 0個使用: +150pt / 1個: +100pt / 2個: +50pt / 3個: +0pt
コンボボーナス = (連続正解数 - 1) × 30pt
                → 2連続: +30pt / 3連続: +60pt / ...
```

### ペナルティ

```
不正解時 = -20pt（0以下にはならない）
コンボ   = 0にリセット
```

### 最大スコア（理論値）

全5問をヒントなし・連続正解の場合：
- 1問目: 100 + 150 + 0 = 250pt
- 2問目: 100 + 150 + 30 = 280pt
- 3問目: 100 + 150 + 60 = 310pt
- 4問目: 100 + 150 + 90 = 340pt
- 5問目: 100 + 150 + 120 = 370pt
- **合計: 1,550pt**

---

## ヒントシステム

### 教育的な目的

「わからない」で終わらせず、段階的にヒントを出して理解を助ける。
ヒントなしで正解するとボーナス点がもらえるので、まず自力で考える動機になる。

### ヒントの表示

| 状態 | 見た目 |
|------|--------|
| 未解放 | 🔒 ヒント1（灰色・破線枠） |
| 解放済み | 💡 主語が三人称単数→動詞にs（金色・実線枠） |

### ヒントの内容ルール

1. ヒント1：一番基本的な文法ルール
2. ヒント2：この問題に当てはまる具体的な説明
3. ヒント3：ほぼ答えに直結する強いヒント

---

## 画面遷移

```
┌──────────────┐
│  スタート画面  │
│  「冒険に出発」│
└──────┬───────┘
       ↓
┌──────────────┐
│  ステージ1    │──→ 正解 ──→ ステージ2 ──→ ... ──→ ステージ5
│  (ゲームプレイ)│          ↑                              │
│              │──→ 不正解 → 4タイル後退                   │
└──────────────┘           → やり直し                      ↓
                                                 ┌──────────────┐
                                                 │   完了画面     │
                                                 │  最終スコア表示│
                                                 │  もう一度プレイ│
                                                 └──────────────┘
```

---

## カメラ（視点）の動き

- プレイヤーを画面の少し下側（55%の位置）に表示
- カメラはプレイヤーを追いかけてスムーズに移動
- 追従の速度：`camY += (player.py - camY) * 0.12`（なめらかに遅れてついてくる）
- プレイヤーの表示位置もスムーズ補間：`player.px += (target - player.px) * 0.3`
